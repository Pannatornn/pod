'use client';

export default function NotFound(){
 return (
  <main className="legacy-section contact-box" style={{minHeight:'60vh',display:'grid',placeItems:'center'}}>
   <div>
    <p className="legacy-kicker">404 / DATABASE NOT FOUND</p>
    <h1>SYSTEM PATH ERROR</h1>
    <p>The requested engineering archive does not exist.</p>
    <a href="/">RETURN TO MAIN SYSTEM ↗</a>
   </div>
  </main>
 );
}
