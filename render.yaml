services:
  - type: web
    name: pod
    runtime: static
    repo: https://github.com/Pannatornn/pod
    branch: main
    buildCommand: npm install && npm run build
    staticPublishPath: ./out
    autoDeployTrigger: commit
